/*
  Time-based mechanic placeholder.

  The time creative director should develop this file later.
  For the base artwork version, these functions exist so the project
  already has the required modular structure.
*/

function setupTimeMechanic() {
  // Add setup code for time-based mechanic here.
}

function updateTimeMechanic() {
  // Keep the base version static for now.
  // Later example:
  // state.waveHeight = map(sin(frameCount * 0.02), -1, 1, 0.85, 1.25);
  // state.crashAmount = map(sin(frameCount * 0.01), -1, 1, 0, 1);
}
